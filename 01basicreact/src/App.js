import Coffeee from './Coffeee.jsx'

function App() {
  return (
    <>
    <Coffeee/>
    <h1>mac aur react</h1>
    </>
  );
}
// here if we didnt give the functinal component names with capitall letter it will show the blank page, so we have to debug by naming the functional component with capital letter first.

export default App;
